const app = Vue.createApp({
  // can add data, functions to react to events we want
  // can add template we want to render in the app element
  // template: '<h2> I am the template! </h2>'
})

app.mount('#app')